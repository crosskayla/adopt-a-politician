<center>
  <div class="container col-sm-10 col-sm-offset-1">
    <h1 class="display-4">Hey there, <%= @voter.username %></h1>
    <br>

    <% if @donations.any? %>
      <p class="lead"><b>Favorite Politician:</b> <%= @voter.favorite_politician %></p>
      <%= render partial: "donations/list", locals: {donations: @donations} %>
    <% else %>
      <p class="lead">No donations yet!</p>
      <a class="btn btn-primary btn-lg" href="<%= politicians_path %>" role="button">Browse politicians</a>
      <a class="btn btn-primary btn-lg" href="<%= donations_path %>" role="button">Browse donations</a>
    <% end %>
  </div>
</center>
